vector<int> indexes;
    // for (int i = 0; i <= (s.size() - p.size()); i++)
    // {

    //     if (isAnagramRange(s, p, i, i + (p.size() - 1)))
    //         indexes.push_back(i);
    // }
    // return indexes;